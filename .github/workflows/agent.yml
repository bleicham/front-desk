name: Front Desk issue agent (disabled)

# GitHub Models was retired on July 30, 2026. Keeping this manual-only file
# overwrites older copies of the automatic issue workflow without spending
# Actions minutes or attempting a retired API call.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: echo "The old GitHub Models issue agent is disabled. The public Pages app now uses verified extractive answers."
